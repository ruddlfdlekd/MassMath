<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  	<mapper namespace="QnaMapper">
  	<select id="selectList" parameterType="ListData" resultType="QnaDTO">
  		select * from
  			(select rownum R, N.* from
  			(select * from qna
  			<if test="kind == 'title'">
				where title LIKE '%'|| #{search}||'%'
			</if>
			<if test="kind == 'writer'">
				where writer LIKE '%'|| #{search}||'%'
			</if>
			<if test="kind == 'contents'">
				where contents LIKE '%'|| #{search}||'%'
			</if>			
  			order by num desc) N)
  			where R between #{startRow} and #{lastRow} and step=0<!-- Map의 key를 써줌 -->
  	</select>
  	<select id="selectOne" parameterType="int" resultType="BoardDTO">
  		select * from qna where num=#{num}
  	</select>
  	
 	<insert id="insert" parameterType="QnaDTO">
 	<selectKey resultType="int" order="BEFORE" keyProperty="num">
		select qna_seq.nextval from dual
	</selectKey>
 		insert into qna values(qna_seq.nextval,#{email},#{writer},#{title},#{contents},sysdate,0,qna_seq.currval,0,0)
 	</insert>
  		
  		
  	</mapper>